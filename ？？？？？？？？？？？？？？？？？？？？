while true do
    local success, result = pcall(function()
        return game:HttpGet(url, true)  
    end)

    if success then
        print(result)
    else
        warn("HttpGet 请求失败：" .. result)
    end
    
    -- 可选：添加短暂延迟，避免过于频繁
    wait(0.000000000000000001)  
end
