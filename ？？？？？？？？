local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local TeleportService = game:GetService("TeleportService")
local SoundService = game:GetService("SoundService")
local TweenService = game:GetService("TweenService")
local localPlayer = Players.LocalPlayer

-- 1. 启动音效（精简版）
local StartSound = Instance.new("Sound")
StartSound.Parent = SoundService
StartSound.SoundId = "rbxassetid://148729028"
StartSound.Volume = 0.5
StartSound:Play()

-- 2. 基础UI容器
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "KakaScriptUI"
ScreenGui.Parent = localPlayer.PlayerGui
ScreenGui.IgnoreGuiInset = true
ScreenGui.DisplayOrder = 100

-- 3. 主窗口（UI优化+稳定）
local MainWin = Instance.new("Frame")
MainWin.Parent = ScreenGui
MainWin.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
MainWin.Position = UDim2.new(0.5, -150, 0.5, -140)
MainWin.Size = UDim2.new(0, 300, 0, 280)
MainWin.ZIndex = 2

-- 窗口优化（精简质感）
local WinCorner = Instance.new("UICorner")
WinCorner.Parent = MainWin
WinCorner.CornerRadius = UDim.new(0, 14)

local WinShadow = Instance.new("UIStroke")
WinShadow.Parent = MainWin
WinShadow.Color = Color3.fromRGB(0, 0, 0)
WinShadow.Transparency = 0.85
WinShadow.Thickness = 8

-- 4. 黑白边框（优化渐变）
local Border = Instance.new("Frame")
Border.Parent = MainWin
Border.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
Border.Position = UDim2.new(0, 2, 0, 2)
Border.Size = UDim2.new(1, -4, 1, -4)
Border.ZIndex = 1

local BorderCorner = Instance.new("UICorner")
BorderCorner.Parent = Border
BorderCorner.CornerRadius = UDim.new(0, 12)

local BorderGradient = Instance.new("UIGradient")
BorderGradient.Parent = Border
BorderGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(255,255,255)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(100,100,100))
})

-- 5. 标题栏（优化交互）
local Title = Instance.new("TextLabel")
Title.Parent = MainWin
Title.BackgroundTransparency = 1
Title.Position = UDim2.new(0, 0, 0.05, 0)
Title.Size = UDim2.new(1, 0, 0, 40)
Title.Font = Enum.Font.GothamBlack
Title.Text = "黑白脚本 - 卡密验证"
Title.TextColor3 = Color3.fromRGB(255,255,255)
Title.TextSize = 18
Title.TextXAlignment = Enum.TextXAlignment.Center
Title.TextYAlignment = Enum.TextYAlignment.Center
Title.ZIndex = 3
Title.Active = true
Title.Selectable = true

-- 6. 每周换卡密提醒（优化醒目度）
local Note = Instance.new("TextLabel")
Note.Parent = MainWin
Note.BackgroundTransparency = 1
Note.Position = UDim2.new(0, 0, 0.2, 0)
Note.Size = UDim2.new(1, 0, 0, 20)
Note.Font = Enum.Font.GothamBold
Note.Text = "⚠️ 每周一换卡密 | 找群主获取"
Note.TextColor3 = Color3.fromRGB(255, 140, 140)
Note.TextSize = 13
Note.TextXAlignment = Enum.TextXAlignment.Center
Note.ZIndex = 3

-- 7. 群聊显示（优化视觉）
local GroupChat = Instance.new("TextLabel")
GroupChat.Parent = MainWin
GroupChat.BackgroundTransparency = 1
GroupChat.Position = UDim2.new(0, 0, 0.3, 0)
GroupChat.Size = UDim2.new(1, 0, 0, 25)
GroupChat.Font = Enum.Font.GothamBold
GroupChat.Text = "📱 群聊获取：1012033070"
GroupChat.TextColor3 = Color3.fromRGB(255,255,255)
GroupChat.TextSize = 15
GroupChat.TextXAlignment = Enum.TextXAlignment.Center
GroupChat.ZIndex = 3

-- 8. 输入框（优化质感+交互）
local Input = Instance.new("TextBox")
Input.Parent = MainWin
Input.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
Input.Position = UDim2.new(0.5, -110, 0.42, 0)
Input.Size = UDim2.new(0, 220, 0, 45)
Input.Font = Enum.Font.Gotham
Input.Text = ""
Input.TextColor3 = Color3.fromRGB(255,255,255)
Input.TextSize = 16
Input.TextXAlignment = Enum.TextXAlignment.Center
Input.PlaceholderText = "输入卡密"
Input.PlaceholderColor3 = Color3.fromRGB(120, 120, 120)
Input.BorderSizePixel = 0
Input.ZIndex = 3

local InputCorner = Instance.new("UICorner")
InputCorner.Parent = Input
InputCorner.CornerRadius = UDim.new(0, 10)

local InputStroke = Instance.new("UIStroke")
InputStroke.Parent = Input
InputStroke.Color = Color3.fromRGB(255,255,255)
InputStroke.Transparency = 0.9
InputStroke.Thickness = 1

-- 聚焦反馈（精简版）
Input.Focused:Connect(function()
    Input.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    InputStroke.Transparency = 0.5
end)
Input.FocusLost:Connect(function()
    Input.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
    InputStroke.Transparency = 0.9
end)

-- 9. 验证按钮（优化质感+交互）
local VerifyBtn = Instance.new("TextButton")
VerifyBtn.Parent = MainWin
VerifyBtn.Position = UDim2.new(0.5, -100, 0.62, 0)
VerifyBtn.Size = UDim2.new(0, 200, 0, 50)
VerifyBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
VerifyBtn.Font = Enum.Font.GothamBold
VerifyBtn.Text = "验证卡密"
VerifyBtn.TextColor3 = Color3.fromRGB(255,255,255)
VerifyBtn.TextSize = 18
VerifyBtn.TextXAlignment = Enum.TextXAlignment.Center
VerifyBtn.BorderSizePixel = 0
VerifyBtn.ZIndex = 3

local BtnCorner = Instance.new("UICorner")
BtnCorner.Parent = VerifyBtn
BtnCorner.CornerRadius = UDim.new(0, 10)

-- 按钮交互（精简版）
VerifyBtn.MouseEnter:Connect(function()
    VerifyBtn.BackgroundColor3 = Color3.fromRGB(80, 80, 80)
end)
VerifyBtn.MouseLeave:Connect(function()
    VerifyBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
end)
VerifyBtn.MouseButton1Down:Connect(function()
    VerifyBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
end)
VerifyBtn.MouseButton1Up:Connect(function()
    VerifyBtn.BackgroundColor3 = Color3.fromRGB(80, 80, 80)
end)

-- 10. 提示信息
local Msg = Instance.new("TextLabel")
Msg.Parent = MainWin
Msg.BackgroundTransparency = 1
Msg.Position = UDim2.new(0, 0, 0.85, 0)
Msg.Size = UDim2.new(1, 0, 0, 25)
Msg.Font = Enum.Font.Gotham
Msg.Text = "卡密联系群主获取"
Msg.TextColor3 = Color3.fromRGB(150,150,150)
Msg.TextSize = 14
Msg.TextXAlignment = Enum.TextXAlignment.Center
Msg.ZIndex = 3

-- 11. 关闭按钮（优化质感+添加消失动画）
local CloseBtn = Instance.new("TextButton")
CloseBtn.Parent = MainWin
CloseBtn.Position = UDim2.new(1, -45, 0.05, 0)
CloseBtn.Size = UDim2.new(0, 38, 0, 38)
CloseBtn.BackgroundColor3 = Color3.fromRGB(50, 20, 20)
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.fromRGB(200,200,200)
CloseBtn.TextSize = 18
CloseBtn.TextXAlignment = Enum.TextXAlignment.Center
CloseBtn.BorderSizePixel = 0
CloseBtn.ZIndex = 10

local CloseCorner = Instance.new("UICorner")
CloseCorner.Parent = CloseBtn
CloseCorner.CornerRadius = UDim.new(1, 0)

CloseBtn.MouseEnter:Connect(function()
    CloseBtn.BackgroundColor3 = Color3.fromRGB(80, 30, 30)
    CloseBtn.TextColor3 = Color3.fromRGB(255,255,255)
end)
CloseBtn.MouseLeave:Connect(function()
    CloseBtn.BackgroundColor3 = Color3.fromRGB(50, 20, 20)
    CloseBtn.TextColor3 = Color3.fromRGB(200,200,200)
end)

-- ========== 从小到大动画（稳定版） ==========
MainWin.Size = UDim2.new(0, 0, 0, 0)
TweenService:Create(MainWin, TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
    Size = UDim2.new(0, 300, 0, 280)
}):Play()

-- ========== 双端拖动（稳定版） ==========
local isDragging = false
local startInputPos = nil
local startWinPos = nil

Title.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        isDragging = true
        startInputPos = input.Position
        startWinPos = MainWin.Position
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if not isDragging then return end
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        local offset = input.Position - startInputPos
        MainWin.Position = UDim2.new(
            startWinPos.X.Scale, startWinPos.X.Offset + offset.X,
            startWinPos.Y.Scale, startWinPos.Y.Offset + offset.Y
        )
    end
end)

UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        isDragging = false
    end
end)

-- ========== 核心功能 ==========
-- 关闭按钮（添加从大到小消失动画）
CloseBtn.MouseButton1Click:Connect(function()
    -- 关闭时执行相同的缩小消失动画
    local closeTween = TweenService:Create(
        MainWin,
        TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.In),
        { Size = UDim2.new(0, 0, 0, 0), Transparency = 1 }
    )
    closeTween:Play()
    closeTween.Completed:Wait() -- 等待动画结束后销毁资源
    ScreenGui:Destroy()
    StartSound:Destroy()
end)

-- 验证按钮（保留从大到小消失动画，删除Teleport语句）
local attempts = 0
local maxAttempts = 3
VerifyBtn.MouseButton1Click:Connect(function()
    local key = Input.Text
    if key == "HB_XiaoXu exactly Gay" then
        Msg.Text = "验证成功，启动中..."
        Msg.TextColor3 = Color3.fromRGB(80, 200, 80)
        task.wait(1)
        
        -- 验证成功时的缩小消失动画
        local closeTween = TweenService:Create(
            MainWin,
            TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.In),
            { Size = UDim2.new(0, 0, 0, 0), Transparency = 1 }
        )
        closeTween:Play()
        closeTween.Completed:Wait()
        
        ScreenGui:Destroy()
        StartSound:Destroy()
        
        -- 加载脚本+调整移动速度
        pcall(function()
            -- 如果文件存在且路径正确，格式应该是：
loadstring(game:HttpGet('https://raw.githubusercontent.com/tfcygvunbind/557/main/天'))()

        end)
        if localPlayer.Character and localPlayer.Character.Humanoid then
            localPlayer.Character.Humanoid.WalkSpeed = 25
        end
    else
        attempts = attempts + 1
        Msg.Text = "错误("..attempts.."/"..maxAttempts..")"
        Msg.TextColor3 = Color3.fromRGB(200, 80, 80)
        for i=1,3 do
            Input.Position = UDim2.new(0.5, -110 + (i%2==1 and 5 or -5), 0.42, 0)
            task.wait(0.05)
        end
        Input.Position = UDim2.new(0.5, -110, 0.42, 0)
        if attempts >= maxAttempts then
            task.wait(1)
            localPlayer:Kick("验证失败")
        end
    end
end)

-- 边框动效（稳定版）
coroutine.wrap(function()
    while Border.Parent do
        for i=0,1,0.01 do
            BorderGradient.Offset = Vector2.new(i,0)
            task.wait(0.02)
        end
        BorderGradient.Offset = Vector2.new(0,0)
    end
end)()
